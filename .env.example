# Public runtime configuration (exposed to the client via runtimeConfig.public)

# Base URL of the backend API
NUXT_PUBLIC_API_BASE=http://localhost:5000/api

# Display name of the platform
NUXT_PUBLIC_APP_NAME=delant
